apiVersion: v1
kind: Service
metadata:
  name: movies-session-service
spec:
  type: ClusterIP
  selector:
    app: movies-session-service
  ports:
    - name: http
      port: 3500
      targetPort: 3500